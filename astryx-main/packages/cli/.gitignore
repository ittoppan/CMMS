# Generated ./api type declarations — emitted from the JSDoc in api/**/*.mjs by
# scripts/sync-api-types.mjs. Not committed (like dist/ in the other packages);
# regenerated automatically at `prepack` so the published tarball always ships
# a fresh, in-sync type surface. Run `pnpm sync:api-types` to produce them locally.
api/**/*.d.mts
api/*.d.mts
.api-verify/

# Transient fixture dir written by authoring-migration.tsc.test.mjs (runs the
# real tsc over migrated output); created + removed per test run.
.codemod-tsc-*/
